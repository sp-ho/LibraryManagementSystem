package com.prog2.labs;

import java.awt.event.ActionEvent;

public class Main {

	/**
	 * Write your test code below in the main (optional).
	 *
	 */
	public static void main(String[] args) {

		AdminLogin.frame = new AdminLogin();
		AdminLogin.frame.setVisible(true);
//		if (login.rdbtnLibrarian.isSelected() && login.btnLogin.action(ActionEvent, login)); // LibrarianRadio selected when login pressed
		// AdminMain view = new AdminMain();
		// Librarian modelL = new Librarian();
		// LibrarianController controller = new LibrarianController(modelL, null, null, view);
		// login.setVisible(false);
		// view.setVisible(true);
		// if (addBooks button is clicked)
		//		AddBook addBook = new AddBook();
		//		addBook.setVisible(true);
		// 		Book book = new Book(dsf, sdf, sdf)
		//      controller.addBook(book, "books") 
	}

	/**
	 * Please refer to he README file for question(s) description
	 */

}
